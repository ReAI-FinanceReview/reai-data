"""배정기 식별자. 구현에서 분리된 리프 모듈이다.

집계 모듈이 식별자 두 개 때문에 배정기 구현을 import 하면 OpenAI 스택이 통째로
따라온다 (실측: ``import src.gold.aggregator`` 0.22s → 1.25s). 식별자는 구현이
아니므로 양쪽이 함께 의존할 수 있는 리프에 둔다.

값은 마이그레이션 20260813_0002 의 ``server_default`` 와 반드시 같아야 한다 —
어긋나면 리비전 이전에 적재된 행이 anti-join 에서 빠져 과거 전량이 조용히
재처리된다. 테스트가 그 일치를 단언한다.
"""

ASSIGNER_RULE = "rule"
ASSIGNER_LLM = "llm"

# 허용 배정기 집합. 배정기를 여기서 파생시키지 않고 따로 열거하지 말 것 —
# 집합이 흩어지면 하나를 추가할 때 빠뜨린 쪽이 테스트가 아니라 운영에서 처음
# 드러난다. 테스트가 이 집합과 실행 레지스트리·CLI 선택지의 일치를 단언한다.
KNOWN_ASSIGNERS = (ASSIGNER_RULE, ASSIGNER_LLM)
